#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../rc/cli.js" "$@"
else
  exec node  "$basedir/../../../rc/cli.js" "$@"
fi
# cmd-shim-target=C:/Users/braya/OneDrive/Escritorio/Electiva III/Taller22/ChatRecursosMoviles/node_modules/.pnpm/registry-url@3.1.0/node_modules/rc/cli.js
