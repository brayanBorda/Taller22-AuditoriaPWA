#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../is-ci/bin.js" "$@"
else
  exec node  "$basedir/../../../is-ci/bin.js" "$@"
fi
# cmd-shim-target=C:/Users/braya/OneDrive/Escritorio/Electiva III/Taller22/ChatRecursosMoviles/node_modules/.pnpm/update-notifier@2.5.0/node_modules/is-ci/bin.js
